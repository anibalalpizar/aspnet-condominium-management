@model IEnumerable<Infraestructure.Model.RUBRO_COBRO>

@{
    ViewBag.Title = "IndexAdmin";
}

<h2>IndexAdmin</h2>

<p>
    <a href="@Url.Action("Create", "RubrosCobros")" class="btn btn-primary"><i class="fas fa-plus"></i> Crear</a>
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.ID_RUBRO_COBRO)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.NOMBRE_RUBRO)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.MONTO)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.DESCRIPCION)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.ID_RUBRO_COBRO)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.NOMBRE_RUBRO)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.MONTO)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.DESCRIPCION)
        </td>
        <td>
            <a href="@Url.Action("Edit", "RubrosCobros", new { id = item.ID_RUBRO_COBRO })" class="btn btn-primary"><i class="fa-solid fa-pen"></i> Editar</a>
            @*@Html.ActionLink("Edit", "Edit", new { /* id=item.PrimaryKey */ }) |*@
            @Html.ActionLink("Details", "Details", new { /* id=item.PrimaryKey */ }) |
            @Html.ActionLink("Delete", "Delete", new { /* id=item.PrimaryKey */ })
        </td>
    </tr>
}

</table>
