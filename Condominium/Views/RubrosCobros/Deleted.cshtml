@model Infraestructure.Model.RUBRO_COBRO

@{
    ViewBag.Title = "Deleted";
}

<h2>Deleted</h2>

<h3>Are you sure you want to delete this?</h3>
<div>
    <h4>RUBRO_COBRO</h4>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.ID_RUBRO_COBRO)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.ID_RUBRO_COBRO)
        </dd>

        <dt>
            @Html.DisplayNameFor(model => model.MONTO)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.MONTO)
        </dd>

        <dt>
            @Html.DisplayNameFor(model => model.IMAGEN)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.IMAGEN)
        </dd>

        <dt>
            @Html.DisplayNameFor(model => model.DESCRIPCION)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.DESCRIPCION)
        </dd>

    </dl>

    @using (Html.BeginForm("Delete", "RubrosCobros", FormMethod.Post, new { enctype = "multipart/form-data" }))
    {
        @Html.AntiForgeryToken()

        <div class="form-actions no-color">
            <input type="submit" value="Delete" class="btn btn-default" /> |
            @Html.ActionLink("Back to List", "Index")
        </div>
    }
</div>
